extends layout

block content
  .row.banner
    .col-12
      h1 F1 Encyclopedia
        small &nbsp;Home page

  .row
    .col-12.col-md-12
      .card
        .card-block
          h4
            a(href=`/races/${last_race}`) Last Race
          p.current-team Ferarri
      .card
        .card-block
          h4
            a(href=`/seasons/${last_season}`)= last_season + " Season"
          p.current-team Ferarri
      .card
        .card-block
          h4
            a(href='/drivers') Drivers
          p.current-team Ferarri
      .card
        .card-block
          h4
            a(href='/teams') Teams
          p.current-team Ferarri
      .card
        .card-block
          h4
            a(href='/circuits') Circuits
          p.current-team Ferarri
        
